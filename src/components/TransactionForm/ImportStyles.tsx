import React from 'react';
import { ensureCssImported } from "../FixedCssImport";

// Ensure CSS is imported
ensureCssImported();

export const ImportStyles: React.FC = () => {
  return null; // This component doesn't render anything, it just imports the CSS
};